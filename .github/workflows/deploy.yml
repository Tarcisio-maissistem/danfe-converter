name: Deploy to Easypanel

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  notify:
    name: Notify Deploy
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy notification
        run: |
          echo "✅ Push detectado na branch main"
          echo "🚀 Easypanel iniciará o deploy automaticamente"
          echo "📦 Commit: ${{ github.sha }}"
          echo "👤 Autor: ${{ github.actor }}"

      - name: Wait for Easypanel
        run: |
          echo "⏳ Aguardando deploy no Easypanel..."
          sleep 60

      - name: Check application health
        run: |
          echo "🔍 Verificando saúde da aplicação..."
          response=$(curl -s -o /dev/null -w "%{http_code}" "https://danfe.maissistem.com.br/health")
          
          if [ "$response" -eq 200 ]; then
            echo "✅ Aplicação está saudável! (HTTP $response)"
            echo "🎉 Deploy concluído com sucesso!"
          else
            echo "⚠️ Status HTTP: $response"
            echo "ℹ️ Verifique os logs no Easypanel"
          fi